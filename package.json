{
  "name": "etosis-website-11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --style=compressed _includes/scss/style.scss _site/style.css",
    "watch:eleventy": "npx @11ty/eleventy --serve",
    "watch:sass": "npm run sass -- --watch",
    "start": "npm-run-all sass --parallel watch:*",
    "build": "npm run sass && npx @11ty/eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "clean-css": "^4.2.3",
    "eleventy-plugin-svg-contents": "^0.7.0",
    "gsap": "^3.5.1",
    "sass": "^1.29.0"
  }
}
